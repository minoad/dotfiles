#!/usr/bin/env bash

echo "use batcat instead of cat"
echo "dirs -v # list of auto dirs."
echo "dirs -v # list of auto dirs."
echo "copypath # copies current path to buffer"
echo "copybuffer; ctrl-o # copies command currently typed into."
echo "copyfile # copies contents of a file to the buffer"
echo "use ff to get a colorized preview of fzf rather than using grep"
echo "ctrl t to select files"
echo "alt c to switch into subdirectory"
echo "ctrl r to get a great smart search of command history"
echo "z <command> to quickly get to common directories"
